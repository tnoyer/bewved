{{ form_start(sessionForm) }}
<div class="card m-4">
    <div class="card-header d-flex justify-content-between align-items-center">
        <p class="h4">{{ title }}</p>
    </div>
    <div class="card-body">
        {{ form_errors(sessionForm) }}
        {{ form_row(sessionForm.name) }}
        {{ form_row(sessionForm.start) }}
        {{ form_row(sessionForm.end) }}
        {{ form_row(sessionForm.size) }}
    </div>
    <div class="card-footer">
        <a href="{{ path('user_manage') }}" class="btn btn-secondary">Retour</a>
        <button class="btn btn-primary">{{ action }}</button>
    </div>
</div>
{{ form_end(sessionForm) }}